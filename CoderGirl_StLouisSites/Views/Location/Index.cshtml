@model IEnumerable<CoderGirl_StLouisSites.Models.Location>

@{
    ViewData["Title"] = "Locations";
}

<h1>Location List</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            @* <th>
            @Html.DisplayNameFor(model => model.Id)
        </th>*@
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.AverageRating)
            </th>*@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            @*<td>
        @Html.DisplayFor(modelItem => item.Id)
            </td>*@
            <td>
                <a class="text-justify table-hover" asp-controller="Location" asp-action="Details" asp-route-Id="@item.Id">@item.Name</a>
                @*@Html.ActionLink("item.Name", "Details", "Location", new { id = item.Id })*@
                @* @Html.DisplayFor(modelItem => item.Name)*@
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            @* USE VIEW MODELS AS A BETTER ARCHITECTURE<td>
            @Html.DisplayFor(modelItem => item.RateAndReviews.Where(r => r.Rating).ToList()  )
        </td>*@
            <td>
                @Html.ActionLink("Rate/Review", "Create", "LocationRateAndReview", new { locationId = item.Id }) |
               @* @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |*@
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
